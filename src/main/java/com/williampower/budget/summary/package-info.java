/**
 * The Summary module maintains a read-optimized view of overall account
 * balance and monthly income/expense totals, derived entirely from
 * {@link com.williampower.budget.transaction.TransactionRecorded} events.
 * It publishes nothing of its own; it is a terminal (leaf) module.
 */
package com.williampower.budget.summary;
